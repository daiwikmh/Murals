import type { ValidatorDto, ValidatorSearchResultDto } from "@stakekit/api-hooks";
import type { Networks } from "@stakekit/common";
export declare const importValidator: ({ address, network, validatorData, }: {
    network: Networks;
    address: string;
    validatorData: {
        integrationId: ValidatorSearchResultDto["integrationId"];
        validator: ValidatorDto;
    };
}) => import("purify-ts").Either<Error, void | undefined>;
