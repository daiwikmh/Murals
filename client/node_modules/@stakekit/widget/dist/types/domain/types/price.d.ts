import type { TokenDto } from "@stakekit/api-hooks";
import type { TokenString } from "./tokens";
export type Price = {
    price: number | undefined;
    price24H: number | undefined;
};
export declare class Prices {
    value: Map<TokenString, Price>;
    constructor(value: Map<TokenString, Price>);
    getByToken(token: TokenDto): import("purify-ts/Maybe").Maybe<Price>;
}
