import type { SKWallet } from "../../domain/types";
import type { Nullable } from "../../types";
type Props = Pick<SKWallet, "address" | "chain" | "isConnected" | "isConnecting">;
export declare const useSyncExternalProvider: ({ address, chain, isConnected, isConnecting, }: { [Key in keyof Props]: Nullable<Props[Key]>; }) => void;
export {};
