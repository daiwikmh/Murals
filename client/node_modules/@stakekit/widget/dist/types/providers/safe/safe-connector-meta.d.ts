import type { GatewayTransactionDetails, SendTransactionsResponse, TransactionStatus } from "@safe-global/safe-apps-sdk";
import type SDK from "@safe-global/safe-apps-sdk";
import type { ConnectorWithFilteredChains } from "@sk-widget/domain/types/connectors";
import type { EitherAsync } from "purify-ts";
import type { Connector } from "wagmi";
export declare const configMeta: {
    id: string;
    name: string;
    type: string;
};
export type ExtraProps = ConnectorWithFilteredChains & {
    getTxStatus(txHash: string): EitherAsync<Error, GatewayTransactionDetails>;
    txStatus: typeof TransactionStatus;
    sendTransactions(...args: Parameters<SDK["txs"]["send"]>): EitherAsync<Error, SendTransactionsResponse>;
};
type SafeConnector = Connector & ExtraProps;
export declare const isSafeConnector: (connector: Connector) => connector is SafeConnector;
export {};
