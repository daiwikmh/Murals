import type { WalletList } from "@stakekit/rainbowkit";
import type { MutableRefObject } from "react";
import type { Connector } from "wagmi";
import type { ConnectorWithFilteredChains } from "../../domain/types/connectors";
import { ExternalProvider } from "../../domain/types/external-providers";
import type { SKExternalProviders } from "../../domain/types/wallets";
type ExtraProps = ConnectorWithFilteredChains & Pick<ExternalProvider, "sendTransaction" | "signMessage"> & {
    onSupportedChainsChanged: (args: {
        supportedChainIds: number[];
        currentChainId: number;
    }) => void;
};
type ExternalConnector = Connector & ExtraProps;
export declare const isExternalProviderConnector: (connector: Connector) => connector is ExternalConnector;
export declare const externalProviderConnector: (variant: MutableRefObject<SKExternalProviders>) => WalletList[number];
export {};
