import type { ActionDto } from "@stakekit/api-hooks";
export declare const useSteps: ({ session, onSignSuccess, }: {
    onSignSuccess?: () => void;
    session: ActionDto;
}) => {
    retry: (() => void) | undefined;
    txStates: {
        state: TxStateEnum;
        tx: import("@stakekit/api-hooks").TransactionDto;
        meta: {
            url: string | null;
            signedTx: string | null;
            broadcasted: boolean | null;
            signError: Error | import("./errors").GetStakeSessionError | import("./errors").SendTransactionError | import("./errors").SignError | null;
            txCheckError: import("./errors").GetStakeSessionError | null;
            done: boolean;
        };
    }[];
};
export declare enum TxStateEnum {
    SIGN_IDLE = 0,
    SIGN_ERROR = 1,
    SIGN_LOADING = 2,
    SIGN_SUCCESS = 3,
    BROADCAST_IDLE = 4,
    BROADCAST_ERROR = 5,
    BROADCAST_LOADING = 6,
    BROADCAST_SUCCESS = 7,
    CHECK_TX_STATUS_IDLE = 8,
    CHECK_TX_STATUS_ERROR = 9,
    CHECK_TX_STATUS_LOADING = 10,
    CHECK_TX_STATUS_SUCCESS = 11
}
