import type { useSteps } from "../hooks/use-steps.hook";
type Props = {
    txState: ReturnType<typeof useSteps>["txStates"][number];
    position: "SINGLE" | "FIRST" | "LAST" | "ELSE";
    count: {
        current: number;
        total: number;
    };
};
export declare const TxState: ({ txState, position, count }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
