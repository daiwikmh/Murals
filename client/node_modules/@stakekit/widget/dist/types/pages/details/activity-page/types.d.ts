import type { ActionDto, YieldDto } from "@stakekit/api-hooks";
import type { TFunction } from "i18next";
export type ActionYieldDto = {
    actionData: ActionDto;
    yieldData: YieldDto;
};
export type DateGroupLabels = "today" | "yesterday" | string;
export declare const dateGroupLabels: (label: DateGroupLabels, t: TFunction) => string;
