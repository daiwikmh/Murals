type Props = {
    isSelected: boolean;
    onTabPress: () => void;
} & ({
    variant: "earn";
    pendingActionsCount?: never;
} | {
    variant: "positions";
    pendingActionsCount?: number;
} | {
    variant: "activity";
    pendingActionsCount?: never;
});
export declare const Tab: ({ isSelected, variant, pendingActionsCount, onTabPress, }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
