import type { RewardTypes, ValidatorDto, YieldDto } from "@stakekit/api-hooks";
import type { ReactNode } from "react";
export declare const useMetaInfo: ({ commission, stakedBalance, stakedBalanceToken, votingPower, address, rewardRate, rewardType, website, }: { [Key in keyof Pick<ValidatorDto, "stakedBalance" | "votingPower" | "commission" | "address" | "website">]: ValidatorDto[Key] | undefined; } & {
    stakedBalanceToken: YieldDto["token"] | undefined;
    rewardRate: number | undefined;
    rewardType: RewardTypes | undefined;
}) => {
    address: {
        title: string;
        val: ReactNode;
    } | null;
    website?: {
        title: string;
        val: ReactNode;
    } | null | undefined;
    stakedBalance?: {
        title: string;
        val: ReactNode;
    } | null | undefined;
    votingPower?: {
        title: string;
        val: ReactNode;
    } | null | undefined;
    commission?: {
        title: string;
        val: ReactNode;
    } | null | undefined;
};
