import { type Dispatch, type PropsWithChildren, type ReactNode, type SetStateAction } from "react";
type State = "full" | "short";
type RootProps = {
    init?: State;
};
declare const Root: ({ children, init }: PropsWithChildren<RootProps>) => import("react/jsx-runtime").JSX.Element;
type AmountProps = {
    fullAmount: string;
    shortAmount: string;
    children?: never;
} | {
    fullAmount?: never;
    shortAmount?: never;
    children: (props: {
        state: State;
    }) => ReactNode;
};
declare const Amount: ({ fullAmount, shortAmount, children }: AmountProps) => import("react/jsx-runtime").JSX.Element;
declare const useAmountContext: () => [State, Dispatch<SetStateAction<State>>];
export { Root, Amount, useAmountContext };
