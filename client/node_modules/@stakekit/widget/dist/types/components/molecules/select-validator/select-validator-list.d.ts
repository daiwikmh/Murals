import type { ValidatorDto, YieldDto } from "@stakekit/api-hooks";
export type GroupedItem = {
    items: ValidatorDto[];
    label: string;
};
export declare const SelectValidatorList: ({ multiSelect, selectedValidators, onItemClick, onViewMoreClick, selectedStake, groupCounts, groupedItems, tableData, }: {
    multiSelect: boolean;
    selectedValidators: Set<ValidatorDto["address"]>;
    onItemClick: (item: ValidatorDto) => void;
    onViewMoreClick: () => void;
    selectedStake: YieldDto;
    tableData: ValidatorDto[];
    groupedItems: GroupedItem[];
    groupCounts: number[];
}) => import("react/jsx-runtime").JSX.Element;
