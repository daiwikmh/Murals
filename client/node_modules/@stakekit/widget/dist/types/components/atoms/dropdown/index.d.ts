interface DropdownProps<T extends {
    label: string;
    value: string;
}> {
    options: T[];
    onSelect: (option: T["value"]) => void;
    selectedOption: T | undefined;
    placeholder: string;
    isError?: boolean;
}
export declare function Dropdown<T extends {
    label: string;
    value: string;
}>({ options, onSelect, selectedOption, placeholder, isError, }: DropdownProps<T>): import("react/jsx-runtime").JSX.Element;
export {};
