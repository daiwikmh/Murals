import type { Atoms } from "@sk-widget/styles";
type TokenIconProps = {
    mainUrl?: string;
    fallbackUrl?: string;
    name: string;
    tokenLogoHw?: Atoms["hw"];
};
export declare const TokenIconImage: ({ mainUrl, fallbackUrl, name, tokenLogoHw, }: TokenIconProps) => import("react/jsx-runtime").JSX.Element;
export {};
