import { type AddressesDto, type TokenDto } from "@stakekit/api-hooks";
import type BigNumber from "bignumber.js";
import { type Maybe } from "purify-ts";
export declare const useGasWarningCheck: (props: {
    gasAmount: Maybe<BigNumber>;
    gasFeeToken: TokenDto;
    address: AddressesDto["address"];
    additionalAddresses: AddressesDto["additionalAddresses"];
    isStake: boolean;
} & ({
    isStake: true;
    stakeAmount: BigNumber;
    stakeToken: TokenDto;
} | {
    isStake: false;
})) => import("@tanstack/react-query").UseQueryResult<boolean, Error>;
