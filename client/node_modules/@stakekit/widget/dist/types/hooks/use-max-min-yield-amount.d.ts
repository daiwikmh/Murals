import type { PositionsData } from "@sk-widget/domain/types/positions";
import type { YieldDto } from "@stakekit/api-hooks";
import BigNumber from "bignumber.js";
import type { Maybe } from "purify-ts";
type Args = {
    yieldOpportunity: Maybe<YieldDto>;
    availableAmount: Maybe<BigNumber>;
} & ({
    type: "enter";
    positionsData: PositionsData;
} | {
    type: "exit";
    positionsData?: never;
});
export declare const useMaxMinYieldAmount: ({ type, yieldOpportunity, availableAmount, positionsData, }: Args) => {
    minEnterOrExitAmount: BigNumber;
    maxEnterOrExitAmount: BigNumber;
    maxIntegrationAmount: BigNumber;
};
export {};
