import type { State } from "@sk-widget/pages/details/earn-page/state/types";
import type { YieldDto } from "@stakekit/api-hooks";
import { Maybe } from "purify-ts";
export declare const useEstimatedRewards: ({ selectedStake, stakeAmount, selectedValidators, }: {
    selectedStake: Maybe<YieldDto>;
    stakeAmount: State["stakeAmount"];
    selectedValidators: State["selectedValidators"];
}) => import("purify-ts/Maybe").Maybe<{
    percentage: string;
    yearly: string;
    monthly: string;
}>;
