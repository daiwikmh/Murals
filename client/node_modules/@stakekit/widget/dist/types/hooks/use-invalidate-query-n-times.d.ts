import type { QueryKey } from "@tanstack/react-query";
export declare const useInvalidateQueryNTimes: ({ key, shouldRefetch, enabled, queryKey, waitMs, times, }: {
    key: QueryKey;
    enabled: boolean;
    shouldRefetch: () => boolean;
    queryKey: QueryKey;
    waitMs?: number;
    times?: number;
}) => void;
